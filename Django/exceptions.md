## List of Exceptions


every model has its own exceptions, e.g. `Book.objects.get(...)` can raise the `Book.DoesNotExist` exception.

## Exception Handling

Django will raise `500 error` for unhandled exceptions.