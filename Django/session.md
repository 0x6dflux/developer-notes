# Django Session

## ASP.NET Technique
resource: https://learn.microsoft.com/en-us/previous-versions/aspnet/bb386448(v=vs.100)

This technique is used to transport data between pages (to understand the last page data, e.g. whether the user is logged in or not), a `hidden input tag` is placed in the page containing `encrypted data` (not hashed).